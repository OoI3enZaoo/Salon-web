export default function({store,isServer,req,redirect}){
  // export const getUserFromLocalStorage = () => {
  //   const isLogin = window.localStorage.user
  //   return isLogin ? isLogin : false
  // }
    // if (isServer && !req) return
    // const isLogin = window.localStorage.getItem("isLogin")
    // store.commit('setLogin',isLogin)
    // console.log("CheckAuth: " + isLogin);

}
